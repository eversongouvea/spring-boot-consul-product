spring:
  application:
    name: product
  cloud:
    consul:
      discovery:
        instanceId: ${spring.application.name}:${random.value}
      config:
        enabled: true
      
server:
  port: 0
  
        